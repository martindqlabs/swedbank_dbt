version: 2

sources:
  - name: raw_governance_metadata
    schema: RAW_GOVERNANCE_METADATA
    description: "Raw landing zone for the evidence layer: CDE ownership, DQ rule definitions, daily pass/fail history, lineage, and incidents."
    tables:
      - name: cde_registry
      - name: dq_rule_catalog
      - name: dq_check_results_log
      - name: lineage_map
      - name: incident_log
